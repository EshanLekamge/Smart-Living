function loadData(data)
{
		if(data == "btn1")
		{
			
			document.getElementById("paragraph1").innerHTML = "Smart Mirror, only available with our Wash Room system, A two-way mirror, A monitor, Smart mirror can control lighting system";
		}
		else if(data == "btn2")
		{
			
			document.getElementById("paragraph2").innerHTML = "Smart Bath Tub, only available with our Wash Room system, Alexa, Automatically set the water temperature,";
		}
		else if(data == "btn3")
		{
			
			document.getElementById("paragraph3").innerHTML = "Smart Toilet, only available with our Wash Room system, Automatic flush, Heated seat, Remote Control";
		}
		
}