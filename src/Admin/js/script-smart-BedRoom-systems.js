function loadData(data)
{
		if(data == "btn1")
		{
			
			document.getElementById("paragraph1").innerHTML = "Smart Wardrobe , only available with our Bed Room system, Keep you clothes and valuables safe, Intelligant lock, Automatic sliding doors ";
		}
		else if(data == "btn2")
		{
			
			document.getElementById("paragraph2").innerHTML = "Smart Table Lamp , only available with our Bed Room system, iOS/Android app remote control, Automatically works when its dark";
		}
		else if(data == "btn3")
		{
			
			document.getElementById("paragraph3").innerHTML = "Smart Alarm Clock, only available with our Bed Room system, iOS/Android app remote control, Wi-fi,Bluetooth connectivity, Can add reminders";
		}
		
}
